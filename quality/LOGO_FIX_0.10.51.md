# Fleet Commander 0.10.51-beta — Logo Fallback Fix

Both header logo positions now show the same pulsing crosshair when no organization logo is configured. After a custom logo is selected, both fallback icons are hidden reliably.

This update does not change fleet data, shared sessions or the language-pack schema.
