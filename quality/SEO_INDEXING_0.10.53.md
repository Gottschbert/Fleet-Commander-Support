# Fleet Commander 0.10.53-beta — SEO and Indexing

The public website now provides one canonical HTTPS WWW address, descriptive search metadata, social sharing metadata and structured `WebApplication` data.

The sitemap lists only the public application homepage. Admin, API, runtime data, encrypted join routes and test areas remain excluded from crawling. This update does not affect saved fleet data or shared sessions.
